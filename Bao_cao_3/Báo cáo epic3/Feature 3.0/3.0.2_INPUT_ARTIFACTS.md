# ĐẦU VÀO EPIC 3 (INPUT ARTIFACTS)
**Tài liệu:** INPUT_ARTIFACTS.md  
**Feature:** 3.0.2 - Chốt input artifacts từ EPIC 2  
**Ngày cập nhật:** 2026-07-28  

Dưới đây là danh sách toàn bộ các artifacts được EPIC 2 bàn giao. EPIC 3 phải tuân thủ sử dụng các file này, tuyệt đối không tạo mới hay tái tạo (re-generate) lại từ mã nguồn.

* **Repository path gốc:** `hitradar/7.ML/7.10.model_packaging/package/`
* **Producer (Thành phần tạo):** EPIC 2 (Data Science & ML Engineering)
* **Consumer (Thành phần sử dụng):** EPIC 3 (FastAPI Backend, Streamlit Frontend)
* **Artifact Package Version:** 2.7.0

## 1. PIPELINE & MODELS
| Tên file | Đường dẫn | Required/Optional | Vai trò | Validation Rule | Checksum (SHA-256) |
|----------|-----------|-------------------|---------|-----------------|--------------------|
| **Full Inference Pipeline** | `pipeline/full_inference_pipeline.joblib` | Required | Canonical pipeline (preprocessing + predict). | Load bằng `joblib` không raise Exception, `predict()` ra kết quả khớp example. | `7ff4b118...` |
| **Best Model** | `models/best_model.joblib` | Optional | Mô hình XGBoost (Champion). | Có thể read attribute `n_estimators`, `get_params()`. | `ea054a9b...` |
| **FE Pipeline** | `preprocessing/feature_engineering_pipeline.joblib` | Optional | Tiền xử lý 18 raw inputs → 31 selected features. | Nằm trong Full Pipeline, không load độc lập. | `...` |
| **Preprocessing Pipeline** | `preprocessing/model_preprocessing_pipeline.joblib` | Optional | Biến đổi 31 features → 49 columns model matrix. | Nằm trong Full Pipeline, không load độc lập. | `...` |

## 2. DỮ LIỆU & SCHEMAS
| Tên file | Đường dẫn | Required/Optional | Vai trò | Validation Rule | Checksum |
|----------|-----------|-------------------|---------|-----------------|----------|
| **Input Schema** | `schemas/input_schema.json` | Required | Cấu trúc 18 trường đầu vào của API. | JSON hợp lệ, chứa đủ 18 keys. | `...` |
| **Output Schema** | `schemas/output_schema.json` | Required | Cấu trúc dữ liệu dự đoán trả về. | JSON hợp lệ, chứa `prediction_raw`. | `...` |
| **Selected Features** | `schemas/selected_features.json` | Required | Danh sách 31 features sau F.E. | JSON Array of strings. | `...` |
| **Feature Names** | `schemas/feature_names.json` | Required | Danh sách 49 transformed columns. | JSON Array of strings. | `...` |
| **Feature Mapping** | `schemas/feature_mapping.json` | Required | Ánh xạ từ raw sang transformed. | Dùng trong SHAP Explainability Service. | `...` |
| **Example Input** | `examples/example_input.json` | Required | Mẫu dữ liệu dùng cho smoke-test. | Truyền vào `/predict` phải trả HTTP 200. | `...` |
| **Example Output** | `examples/example_output.json` | Required | Mẫu kết quả mong đợi. | Khớp kết quả `/predict` (tolerance 1e-5). | `...` |

## 3. METADATA & DEPENDENCIES
| Tên file | Đường dẫn | Required/Optional | Vai trò | Validation Rule | Checksum |
|----------|-----------|-------------------|---------|-----------------|----------|
| **Model Version** | `metadata/model_version.json` | Required | Nhận diện phiên bản model. | Chứa `"version": "1.0.0"`. | `...` |
| **Package Version** | `metadata/package_version.json` | Required | Phiên bản artifact. | Chứa `"version": "2.7.0"`. | `...` |
| **Runtime Req** | `requirements-runtime.txt` | Required | Thư viện phục vụ inference. | Cài đặt `pip install -r` exit code 0. | `...` |
| **Explain Req** | `requirements-explainability.txt` | Required | Thư viện phục vụ SHAP. | Cài đặt `pip install -r` exit code 0. | `...` |

## 4. CODE SERVICES (EPIC 2 HỖ TRỢ)
| Tên file | Đường dẫn | Required/Optional | Vai trò | Validation Rule | Checksum |
|----------|-----------|-------------------|---------|-----------------|----------|
| **Explain Service** | `services/explain_prediction.py` | Required | Sinh SHAP outputs. | Hàm `explain_one_prediction()` chạy không lỗi. | `...` |
| **Inference Wrapper** | `runtime/inference_pipeline.py` | Required | Wrapper class `HitRadarInferencePipeline`. | Class định nghĩa đúng. | `...` |
